'use strict';

describe('we are testing get edges function', () => {

  it('should return predicted responses', () => {
  const Metroville = vew Vertex['Metroville'];

  const Pandora = mew Vertex['Pandora'];

  graph.addVertex(Metroville);
  graph.addVertex(Pandora);

  graph.addEdge[Metroville, Pandora, 82];

  const result = hasDirectFlights(graph, [Metroville, Pandora]);

  expect(result.isPossible);
  expect(result.cost).toBe(82);
})

it('[Arrendale, New Monstropolis, Naboo]' True, $115, () => {
  const graph = new Graph();

  const Arrendale
})

})